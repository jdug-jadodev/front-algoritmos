# Plan Activo

> Plan en ejecución. Cuando se cierra, se mueve a `context/archive/plans/YYYY-MM-PLAN-XXX.md` y este archivo se **resetea** a estado de plantilla.
> **Categoría:** 🟡 Vivo — se resetea al cerrar. Política completa en [`context/lifecycle.md`](lifecycle.md).

## Metadata

- **ID del plan:** PLAN-20260616-01
- **Tarea:** Cambiar "¿Cómo funciona?" → "Ejemplos de código" (JS/Java/Python) y simplificar cabecera del algoritmo (quitar descripción, complejidad, tags)
- **Orquestador:** MiniMax M3 (MACS)
- **Fecha inicio:** 2026-06-16 12:05
- **Estado:** in_progress

## Clasificación

**media** — Afecta 4 archivos (AlgoLayout, Visualizer, ComingSoon, nuevo CodeExamples) + script generador de datos.

## Agentes involucrados

1. **architect** (delegación rápida) — Decidir dónde vive la fuente de datos de los ejemplos (`algorithms.json` vs. componente generador)
2. **senior-dev** — Implementación (Componente `CodeExamples.astro`, script generador, ajustes de `Visualizer.astro` y `ComingSoon.astro`, simplificación de cabecera en `AlgoLayout.astro`)
3. **qa-security** — Validar que el build sigue compilando y los estilos no se rompen

## Plan de ejecución

1. [x] Inspeccionar `algorithms.json`, `AlgoLayout.astro`, `Visualizer.astro`, `ComingSoon.astro`
2. [x] Decidir estrategia de datos: añadir `codeExamples: { js, java, python }` a cada uno de los 110 algoritmos en `algorithms.json`
3. [ ] Escribir `scripts/generate-code-examples.cjs` con implementaciones canónicas breves en 3 lenguajes para los 110 algoritmos
4. [ ] Ejecutar el script para regenerar `algorithms.json` con los `codeExamples`
5. [ ] Crear `src/components/visualizer/CodeExamples.astro` (3 bloques apilados, conservando el estilo `info-card` y `muted`)
6. [ ] En `Visualizer.astro`: renombrar tab `how` → `code` con label "💻 Ejemplos de código" y enchufar el componente
7. [ ] En `ComingSoon.astro`: añadir también la sección de ejemplos de código
8. [ ] En `AlgoLayout.astro`: eliminar bloques de `description`, `complexity` y `tags` del header; dejar solo `<h1>{title}</h1>` y `<LevelBadge />`
9. [ ] Compilar y validar visualmente

## Paralelizaciones

- Paso 5 (componente `CodeExamples.astro`) puede escribirse en paralelo con los pasos 6/7/8, pero la integración depende de que el JSON ya tenga los datos.

## Criterios de éxito

- [ ] El tab "¿Cómo funciona?" deja de existir; aparece "💻 Ejemplos de código"
- [ ] El tab de ejemplos muestra 3 bloques de código (JavaScript, Java, Python) con el mismo lenguaje de estilo
- [ ] La cabecera de la página del algoritmo muestra solo el título y el badge de nivel
- [ ] El proyecto compila sin errores (`npm run build` o `astro build`)
- [ ] Los 110 algoritmos tienen al menos un esqueleto de ejemplo en los 3 lenguajes

## Outputs generados

| Agente | Archivo de output | Estado |
|--------|-------------------|--------|
| orchestrator | `agents/orchestrator/outputs/PLAN-20260616-01.md` | (este archivo) |
| senior-dev | `agents/senior-dev/outputs/PLAN-20260616-01.md` | pendiente |
| qa-security | `agents/qa-security/outputs/PLAN-20260616-01.md` | pendiente |

## Bloqueos activos

- Ninguno.

## Decisiones tomadas durante el plan

- **Decisión 1:** Los ejemplos viven en `algorithms.json` (mismo sitio que `description`, `complexity`, etc.) para mantener la fuente de verdad unificada. Se genera vía script Node idempotente.
- **Decisión 2:** El componente `CodeExamples.astro` recibe las 3 strings como prop (`js`, `java`, `python`) y renderiza bloques `<pre><code>` con etiqueta del lenguaje, manteniendo la estética `info-card` y la tipografía monoespaciada (`Space Mono`) ya presente.
- **Decisión 3:** `ComingSoon.astro` también incluye los ejemplos (no solo los algoritmos con visualizador), porque el usuario pidió "en los algoritmos" en general.

## Resultado final (al cerrar)

_Pendiente_

## Lecciones aprendidas (para `lessons-learned.md`)

_Pendiente_

---

## 🧹 Acciones de cierre (ejecutadas por el Orquestador)

Al pasar este plan a estado `completed`, el Orquestador ejecuta:

- [ ] Mover este archivo a `context/archive/plans/YYYY-MM-PLAN-XXX.md`
- [ ] Agregar encabezado de archivo con fecha y link al plan original
- [ ] Resetear `active-plan.md` a esta plantilla
- [ ] Si hubo decisión arquitectónica → ADR en `decisions-log.md`
- [ ] Si cambió la arquitectura → actualizar `architecture.md` y archivar versión previa
- [ ] Agregar entrada a `shared-context.md` cerrando el plan
- [ ] Si hay lecciones → agregar a `lessons-learned.md`
